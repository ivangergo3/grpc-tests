// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               v3.19.1
// source: acme/shipping/v1/shipping_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Actor, Address, RequestContext } from "../../common/v1/common";

export const protobufPackage = "acme.shipping.v1";

export interface ShipmentItem {
  sku: string;
  quantity: number;
  attributes: { [key: string]: string };
}

export interface ShipmentItem_AttributesEntry {
  key: string;
  value: string;
}

export interface Shipment {
  shipmentId: string;
  orderId: string;
  carrier: string;
  status: string;
  items: ShipmentItem[];
  destination?: Address | undefined;
  metadata: { [key: string]: string };
}

export interface Shipment_MetadataEntry {
  key: string;
  value: string;
}

export interface CreateShipmentRequest {
  shipmentId: string;
  orderId: string;
  items: ShipmentItem[];
  destination?: Address | undefined;
  context?: RequestContext | undefined;
  actor?: Actor | undefined;
  headers: { [key: string]: string };
}

export interface CreateShipmentRequest_HeadersEntry {
  key: string;
  value: string;
}

export interface CreateShipmentResponse {
  shipment?: Shipment | undefined;
  context?: RequestContext | undefined;
}

export interface TrackingEvent {
  eventId: string;
  timestamp: string;
  location: string;
  description: string;
  attributes: { [key: string]: string };
}

export interface TrackingEvent_AttributesEntry {
  key: string;
  value: string;
}

export interface TrackShipmentRequest {
  shipmentId: string;
  context?: RequestContext | undefined;
  headers: { [key: string]: string };
}

export interface TrackShipmentRequest_HeadersEntry {
  key: string;
  value: string;
}

export interface TrackShipmentResponse {
  shipment?: Shipment | undefined;
  events: TrackingEvent[];
  context?: RequestContext | undefined;
}

function createBaseShipmentItem(): ShipmentItem {
  return { sku: "", quantity: 0, attributes: {} };
}

export const ShipmentItem: MessageFns<ShipmentItem> = {
  encode(message: ShipmentItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sku !== "") {
      writer.uint32(10).string(message.sku);
    }
    if (message.quantity !== 0) {
      writer.uint32(16).int32(message.quantity);
    }
    globalThis.Object.entries(message.attributes).forEach(([key, value]: [string, string]) => {
      ShipmentItem_AttributesEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShipmentItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShipmentItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sku = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quantity = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ShipmentItem_AttributesEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.attributes[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShipmentItem {
    return {
      sku: isSet(object.sku) ? globalThis.String(object.sku) : "",
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
      attributes: isObject(object.attributes)
        ? (globalThis.Object.entries(object.attributes) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ShipmentItem): unknown {
    const obj: any = {};
    if (message.sku !== "") {
      obj.sku = message.sku;
    }
    if (message.quantity !== 0) {
      obj.quantity = Math.round(message.quantity);
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [string, string][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShipmentItem>, I>>(base?: I): ShipmentItem {
    return ShipmentItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShipmentItem>, I>>(object: I): ShipmentItem {
    const message = createBaseShipmentItem();
    message.sku = object.sku ?? "";
    message.quantity = object.quantity ?? 0;
    message.attributes = (globalThis.Object.entries(object.attributes ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseShipmentItem_AttributesEntry(): ShipmentItem_AttributesEntry {
  return { key: "", value: "" };
}

export const ShipmentItem_AttributesEntry: MessageFns<ShipmentItem_AttributesEntry> = {
  encode(message: ShipmentItem_AttributesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShipmentItem_AttributesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShipmentItem_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShipmentItem_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ShipmentItem_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShipmentItem_AttributesEntry>, I>>(base?: I): ShipmentItem_AttributesEntry {
    return ShipmentItem_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShipmentItem_AttributesEntry>, I>>(object: I): ShipmentItem_AttributesEntry {
    const message = createBaseShipmentItem_AttributesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseShipment(): Shipment {
  return { shipmentId: "", orderId: "", carrier: "", status: "", items: [], destination: undefined, metadata: {} };
}

export const Shipment: MessageFns<Shipment> = {
  encode(message: Shipment, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shipmentId !== "") {
      writer.uint32(10).string(message.shipmentId);
    }
    if (message.orderId !== "") {
      writer.uint32(18).string(message.orderId);
    }
    if (message.carrier !== "") {
      writer.uint32(26).string(message.carrier);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    for (const v of message.items) {
      ShipmentItem.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.destination !== undefined) {
      Address.encode(message.destination, writer.uint32(50).fork()).join();
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      Shipment_MetadataEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Shipment {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShipment();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shipmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.carrier = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.items.push(ShipmentItem.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.destination = Address.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = Shipment_MetadataEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.metadata[entry7.key] = entry7.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Shipment {
    return {
      shipmentId: isSet(object.shipmentId)
        ? globalThis.String(object.shipmentId)
        : isSet(object.shipment_id)
        ? globalThis.String(object.shipment_id)
        : "",
      orderId: isSet(object.orderId)
        ? globalThis.String(object.orderId)
        : isSet(object.order_id)
        ? globalThis.String(object.order_id)
        : "",
      carrier: isSet(object.carrier) ? globalThis.String(object.carrier) : "",
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ShipmentItem.fromJSON(e)) : [],
      destination: isSet(object.destination) ? Address.fromJSON(object.destination) : undefined,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Shipment): unknown {
    const obj: any = {};
    if (message.shipmentId !== "") {
      obj.shipmentId = message.shipmentId;
    }
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.carrier !== "") {
      obj.carrier = message.carrier;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => ShipmentItem.toJSON(e));
    }
    if (message.destination !== undefined) {
      obj.destination = Address.toJSON(message.destination);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Shipment>, I>>(base?: I): Shipment {
    return Shipment.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Shipment>, I>>(object: I): Shipment {
    const message = createBaseShipment();
    message.shipmentId = object.shipmentId ?? "";
    message.orderId = object.orderId ?? "";
    message.carrier = object.carrier ?? "";
    message.status = object.status ?? "";
    message.items = object.items?.map((e) => ShipmentItem.fromPartial(e)) || [];
    message.destination = (object.destination !== undefined && object.destination !== null)
      ? Address.fromPartial(object.destination)
      : undefined;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseShipment_MetadataEntry(): Shipment_MetadataEntry {
  return { key: "", value: "" };
}

export const Shipment_MetadataEntry: MessageFns<Shipment_MetadataEntry> = {
  encode(message: Shipment_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Shipment_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShipment_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Shipment_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Shipment_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Shipment_MetadataEntry>, I>>(base?: I): Shipment_MetadataEntry {
    return Shipment_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Shipment_MetadataEntry>, I>>(object: I): Shipment_MetadataEntry {
    const message = createBaseShipment_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateShipmentRequest(): CreateShipmentRequest {
  return {
    shipmentId: "",
    orderId: "",
    items: [],
    destination: undefined,
    context: undefined,
    actor: undefined,
    headers: {},
  };
}

export const CreateShipmentRequest: MessageFns<CreateShipmentRequest> = {
  encode(message: CreateShipmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shipmentId !== "") {
      writer.uint32(10).string(message.shipmentId);
    }
    if (message.orderId !== "") {
      writer.uint32(18).string(message.orderId);
    }
    for (const v of message.items) {
      ShipmentItem.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.destination !== undefined) {
      Address.encode(message.destination, writer.uint32(34).fork()).join();
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(42).fork()).join();
    }
    if (message.actor !== undefined) {
      Actor.encode(message.actor, writer.uint32(50).fork()).join();
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]: [string, string]) => {
      CreateShipmentRequest_HeadersEntry.encode({ key: key as any, value }, writer.uint32(58).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateShipmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateShipmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shipmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.items.push(ShipmentItem.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.destination = Address.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.actor = Actor.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          const entry7 = CreateShipmentRequest_HeadersEntry.decode(reader, reader.uint32());
          if (entry7.value !== undefined) {
            message.headers[entry7.key] = entry7.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateShipmentRequest {
    return {
      shipmentId: isSet(object.shipmentId)
        ? globalThis.String(object.shipmentId)
        : isSet(object.shipment_id)
        ? globalThis.String(object.shipment_id)
        : "",
      orderId: isSet(object.orderId)
        ? globalThis.String(object.orderId)
        : isSet(object.order_id)
        ? globalThis.String(object.order_id)
        : "",
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ShipmentItem.fromJSON(e)) : [],
      destination: isSet(object.destination) ? Address.fromJSON(object.destination) : undefined,
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      actor: isSet(object.actor) ? Actor.fromJSON(object.actor) : undefined,
      headers: isObject(object.headers)
        ? (globalThis.Object.entries(object.headers) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: CreateShipmentRequest): unknown {
    const obj: any = {};
    if (message.shipmentId !== "") {
      obj.shipmentId = message.shipmentId;
    }
    if (message.orderId !== "") {
      obj.orderId = message.orderId;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => ShipmentItem.toJSON(e));
    }
    if (message.destination !== undefined) {
      obj.destination = Address.toJSON(message.destination);
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.actor !== undefined) {
      obj.actor = Actor.toJSON(message.actor);
    }
    if (message.headers) {
      const entries = globalThis.Object.entries(message.headers) as [string, string][];
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateShipmentRequest>, I>>(base?: I): CreateShipmentRequest {
    return CreateShipmentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateShipmentRequest>, I>>(object: I): CreateShipmentRequest {
    const message = createBaseCreateShipmentRequest();
    message.shipmentId = object.shipmentId ?? "";
    message.orderId = object.orderId ?? "";
    message.items = object.items?.map((e) => ShipmentItem.fromPartial(e)) || [];
    message.destination = (object.destination !== undefined && object.destination !== null)
      ? Address.fromPartial(object.destination)
      : undefined;
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.actor = (object.actor !== undefined && object.actor !== null) ? Actor.fromPartial(object.actor) : undefined;
    message.headers = (globalThis.Object.entries(object.headers ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseCreateShipmentRequest_HeadersEntry(): CreateShipmentRequest_HeadersEntry {
  return { key: "", value: "" };
}

export const CreateShipmentRequest_HeadersEntry: MessageFns<CreateShipmentRequest_HeadersEntry> = {
  encode(message: CreateShipmentRequest_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateShipmentRequest_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateShipmentRequest_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateShipmentRequest_HeadersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: CreateShipmentRequest_HeadersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateShipmentRequest_HeadersEntry>, I>>(
    base?: I,
  ): CreateShipmentRequest_HeadersEntry {
    return CreateShipmentRequest_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateShipmentRequest_HeadersEntry>, I>>(
    object: I,
  ): CreateShipmentRequest_HeadersEntry {
    const message = createBaseCreateShipmentRequest_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseCreateShipmentResponse(): CreateShipmentResponse {
  return { shipment: undefined, context: undefined };
}

export const CreateShipmentResponse: MessageFns<CreateShipmentResponse> = {
  encode(message: CreateShipmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shipment !== undefined) {
      Shipment.encode(message.shipment, writer.uint32(10).fork()).join();
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateShipmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateShipmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shipment = Shipment.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateShipmentResponse {
    return {
      shipment: isSet(object.shipment) ? Shipment.fromJSON(object.shipment) : undefined,
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
    };
  },

  toJSON(message: CreateShipmentResponse): unknown {
    const obj: any = {};
    if (message.shipment !== undefined) {
      obj.shipment = Shipment.toJSON(message.shipment);
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateShipmentResponse>, I>>(base?: I): CreateShipmentResponse {
    return CreateShipmentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateShipmentResponse>, I>>(object: I): CreateShipmentResponse {
    const message = createBaseCreateShipmentResponse();
    message.shipment = (object.shipment !== undefined && object.shipment !== null)
      ? Shipment.fromPartial(object.shipment)
      : undefined;
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

function createBaseTrackingEvent(): TrackingEvent {
  return { eventId: "", timestamp: "", location: "", description: "", attributes: {} };
}

export const TrackingEvent: MessageFns<TrackingEvent> = {
  encode(message: TrackingEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.eventId !== "") {
      writer.uint32(10).string(message.eventId);
    }
    if (message.timestamp !== "") {
      writer.uint32(18).string(message.timestamp);
    }
    if (message.location !== "") {
      writer.uint32(26).string(message.location);
    }
    if (message.description !== "") {
      writer.uint32(34).string(message.description);
    }
    globalThis.Object.entries(message.attributes).forEach(([key, value]: [string, string]) => {
      TrackingEvent_AttributesEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackingEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackingEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.eventId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.timestamp = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.location = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = TrackingEvent_AttributesEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.attributes[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackingEvent {
    return {
      eventId: isSet(object.eventId)
        ? globalThis.String(object.eventId)
        : isSet(object.event_id)
        ? globalThis.String(object.event_id)
        : "",
      timestamp: isSet(object.timestamp) ? globalThis.String(object.timestamp) : "",
      location: isSet(object.location) ? globalThis.String(object.location) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      attributes: isObject(object.attributes)
        ? (globalThis.Object.entries(object.attributes) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: TrackingEvent): unknown {
    const obj: any = {};
    if (message.eventId !== "") {
      obj.eventId = message.eventId;
    }
    if (message.timestamp !== "") {
      obj.timestamp = message.timestamp;
    }
    if (message.location !== "") {
      obj.location = message.location;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [string, string][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackingEvent>, I>>(base?: I): TrackingEvent {
    return TrackingEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackingEvent>, I>>(object: I): TrackingEvent {
    const message = createBaseTrackingEvent();
    message.eventId = object.eventId ?? "";
    message.timestamp = object.timestamp ?? "";
    message.location = object.location ?? "";
    message.description = object.description ?? "";
    message.attributes = (globalThis.Object.entries(object.attributes ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseTrackingEvent_AttributesEntry(): TrackingEvent_AttributesEntry {
  return { key: "", value: "" };
}

export const TrackingEvent_AttributesEntry: MessageFns<TrackingEvent_AttributesEntry> = {
  encode(message: TrackingEvent_AttributesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackingEvent_AttributesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackingEvent_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackingEvent_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: TrackingEvent_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackingEvent_AttributesEntry>, I>>(base?: I): TrackingEvent_AttributesEntry {
    return TrackingEvent_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackingEvent_AttributesEntry>, I>>(
    object: I,
  ): TrackingEvent_AttributesEntry {
    const message = createBaseTrackingEvent_AttributesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseTrackShipmentRequest(): TrackShipmentRequest {
  return { shipmentId: "", context: undefined, headers: {} };
}

export const TrackShipmentRequest: MessageFns<TrackShipmentRequest> = {
  encode(message: TrackShipmentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shipmentId !== "") {
      writer.uint32(10).string(message.shipmentId);
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(18).fork()).join();
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]: [string, string]) => {
      TrackShipmentRequest_HeadersEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackShipmentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackShipmentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shipmentId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = TrackShipmentRequest_HeadersEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.headers[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackShipmentRequest {
    return {
      shipmentId: isSet(object.shipmentId)
        ? globalThis.String(object.shipmentId)
        : isSet(object.shipment_id)
        ? globalThis.String(object.shipment_id)
        : "",
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      headers: isObject(object.headers)
        ? (globalThis.Object.entries(object.headers) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: TrackShipmentRequest): unknown {
    const obj: any = {};
    if (message.shipmentId !== "") {
      obj.shipmentId = message.shipmentId;
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.headers) {
      const entries = globalThis.Object.entries(message.headers) as [string, string][];
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackShipmentRequest>, I>>(base?: I): TrackShipmentRequest {
    return TrackShipmentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackShipmentRequest>, I>>(object: I): TrackShipmentRequest {
    const message = createBaseTrackShipmentRequest();
    message.shipmentId = object.shipmentId ?? "";
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.headers = (globalThis.Object.entries(object.headers ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseTrackShipmentRequest_HeadersEntry(): TrackShipmentRequest_HeadersEntry {
  return { key: "", value: "" };
}

export const TrackShipmentRequest_HeadersEntry: MessageFns<TrackShipmentRequest_HeadersEntry> = {
  encode(message: TrackShipmentRequest_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackShipmentRequest_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackShipmentRequest_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackShipmentRequest_HeadersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: TrackShipmentRequest_HeadersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackShipmentRequest_HeadersEntry>, I>>(
    base?: I,
  ): TrackShipmentRequest_HeadersEntry {
    return TrackShipmentRequest_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackShipmentRequest_HeadersEntry>, I>>(
    object: I,
  ): TrackShipmentRequest_HeadersEntry {
    const message = createBaseTrackShipmentRequest_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseTrackShipmentResponse(): TrackShipmentResponse {
  return { shipment: undefined, events: [], context: undefined };
}

export const TrackShipmentResponse: MessageFns<TrackShipmentResponse> = {
  encode(message: TrackShipmentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shipment !== undefined) {
      Shipment.encode(message.shipment, writer.uint32(10).fork()).join();
    }
    for (const v of message.events) {
      TrackingEvent.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TrackShipmentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTrackShipmentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shipment = Shipment.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.events.push(TrackingEvent.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TrackShipmentResponse {
    return {
      shipment: isSet(object.shipment) ? Shipment.fromJSON(object.shipment) : undefined,
      events: globalThis.Array.isArray(object?.events) ? object.events.map((e: any) => TrackingEvent.fromJSON(e)) : [],
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
    };
  },

  toJSON(message: TrackShipmentResponse): unknown {
    const obj: any = {};
    if (message.shipment !== undefined) {
      obj.shipment = Shipment.toJSON(message.shipment);
    }
    if (message.events?.length) {
      obj.events = message.events.map((e) => TrackingEvent.toJSON(e));
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TrackShipmentResponse>, I>>(base?: I): TrackShipmentResponse {
    return TrackShipmentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TrackShipmentResponse>, I>>(object: I): TrackShipmentResponse {
    const message = createBaseTrackShipmentResponse();
    message.shipment = (object.shipment !== undefined && object.shipment !== null)
      ? Shipment.fromPartial(object.shipment)
      : undefined;
    message.events = object.events?.map((e) => TrackingEvent.fromPartial(e)) || [];
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

export type ShippingServiceService = typeof ShippingServiceService;
export const ShippingServiceService = {
  createShipment: {
    path: "/acme.shipping.v1.ShippingService/CreateShipment",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: CreateShipmentRequest): Buffer =>
      Buffer.from(CreateShipmentRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): CreateShipmentRequest => CreateShipmentRequest.decode(value),
    responseSerialize: (value: CreateShipmentResponse): Buffer =>
      Buffer.from(CreateShipmentResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): CreateShipmentResponse => CreateShipmentResponse.decode(value),
  },
  trackShipment: {
    path: "/acme.shipping.v1.ShippingService/TrackShipment",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: TrackShipmentRequest): Buffer => Buffer.from(TrackShipmentRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): TrackShipmentRequest => TrackShipmentRequest.decode(value),
    responseSerialize: (value: TrackShipmentResponse): Buffer =>
      Buffer.from(TrackShipmentResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): TrackShipmentResponse => TrackShipmentResponse.decode(value),
  },
} as const;

export interface ShippingServiceServer extends UntypedServiceImplementation {
  createShipment: handleUnaryCall<CreateShipmentRequest, CreateShipmentResponse>;
  trackShipment: handleUnaryCall<TrackShipmentRequest, TrackShipmentResponse>;
}

export interface ShippingServiceClient extends Client {
  createShipment(
    request: CreateShipmentRequest,
    callback: (error: ServiceError | null, response: CreateShipmentResponse) => void,
  ): ClientUnaryCall;
  createShipment(
    request: CreateShipmentRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: CreateShipmentResponse) => void,
  ): ClientUnaryCall;
  createShipment(
    request: CreateShipmentRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: CreateShipmentResponse) => void,
  ): ClientUnaryCall;
  trackShipment(
    request: TrackShipmentRequest,
    callback: (error: ServiceError | null, response: TrackShipmentResponse) => void,
  ): ClientUnaryCall;
  trackShipment(
    request: TrackShipmentRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: TrackShipmentResponse) => void,
  ): ClientUnaryCall;
  trackShipment(
    request: TrackShipmentRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: TrackShipmentResponse) => void,
  ): ClientUnaryCall;
}

export const ShippingServiceClient = makeGenericClientConstructor(
  ShippingServiceService,
  "acme.shipping.v1.ShippingService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): ShippingServiceClient;
  service: typeof ShippingServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
