// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.0
//   protoc               v3.19.1
// source: acme/inventory/v1/inventory_service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Actor, RequestContext } from "../../common/v1/common";

export const protobufPackage = "acme.inventory.v1";

export interface WarehouseRef {
  warehouseId: string;
  region: string;
}

export interface StockItem {
  sku: string;
  available: number;
  reserved: number;
  warehouse?: WarehouseRef | undefined;
  attributes: { [key: string]: string };
}

export interface StockItem_AttributesEntry {
  key: string;
  value: string;
}

export interface GetStockRequest {
  skus: string[];
  region: string;
  context?: RequestContext | undefined;
  actor?: Actor | undefined;
  headers: { [key: string]: string };
}

export interface GetStockRequest_HeadersEntry {
  key: string;
  value: string;
}

export interface GetStockResponse {
  items: StockItem[];
  context?: RequestContext | undefined;
}

/** One message containing N logical responses (for multi-response / aggregated testing). */
export interface GetStockAggregatedResponse {
  responses: GetStockResponse[];
}

export interface ReservationLine {
  sku: string;
  quantity: number;
  attributes: { [key: string]: string };
}

export interface ReservationLine_AttributesEntry {
  key: string;
  value: string;
}

export interface ReserveStockRequest {
  reservationId: string;
  lines: ReservationLine[];
  region: string;
  context?: RequestContext | undefined;
  actor?: Actor | undefined;
  headers: { [key: string]: string };
}

export interface ReserveStockRequest_HeadersEntry {
  key: string;
  value: string;
}

export interface ReserveStockResponse {
  reservationId: string;
  reservedItems: StockItem[];
  context?: RequestContext | undefined;
}

function createBaseWarehouseRef(): WarehouseRef {
  return { warehouseId: "", region: "" };
}

export const WarehouseRef: MessageFns<WarehouseRef> = {
  encode(message: WarehouseRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.warehouseId !== "") {
      writer.uint32(10).string(message.warehouseId);
    }
    if (message.region !== "") {
      writer.uint32(18).string(message.region);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WarehouseRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWarehouseRef();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.warehouseId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.region = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WarehouseRef {
    return {
      warehouseId: isSet(object.warehouseId)
        ? globalThis.String(object.warehouseId)
        : isSet(object.warehouse_id)
        ? globalThis.String(object.warehouse_id)
        : "",
      region: isSet(object.region) ? globalThis.String(object.region) : "",
    };
  },

  toJSON(message: WarehouseRef): unknown {
    const obj: any = {};
    if (message.warehouseId !== "") {
      obj.warehouseId = message.warehouseId;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WarehouseRef>, I>>(base?: I): WarehouseRef {
    return WarehouseRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WarehouseRef>, I>>(object: I): WarehouseRef {
    const message = createBaseWarehouseRef();
    message.warehouseId = object.warehouseId ?? "";
    message.region = object.region ?? "";
    return message;
  },
};

function createBaseStockItem(): StockItem {
  return { sku: "", available: 0, reserved: 0, warehouse: undefined, attributes: {} };
}

export const StockItem: MessageFns<StockItem> = {
  encode(message: StockItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sku !== "") {
      writer.uint32(10).string(message.sku);
    }
    if (message.available !== 0) {
      writer.uint32(16).int32(message.available);
    }
    if (message.reserved !== 0) {
      writer.uint32(24).int32(message.reserved);
    }
    if (message.warehouse !== undefined) {
      WarehouseRef.encode(message.warehouse, writer.uint32(34).fork()).join();
    }
    globalThis.Object.entries(message.attributes).forEach(([key, value]: [string, string]) => {
      StockItem_AttributesEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StockItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStockItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sku = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.available = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reserved = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.warehouse = WarehouseRef.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = StockItem_AttributesEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.attributes[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StockItem {
    return {
      sku: isSet(object.sku) ? globalThis.String(object.sku) : "",
      available: isSet(object.available) ? globalThis.Number(object.available) : 0,
      reserved: isSet(object.reserved) ? globalThis.Number(object.reserved) : 0,
      warehouse: isSet(object.warehouse) ? WarehouseRef.fromJSON(object.warehouse) : undefined,
      attributes: isObject(object.attributes)
        ? (globalThis.Object.entries(object.attributes) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: StockItem): unknown {
    const obj: any = {};
    if (message.sku !== "") {
      obj.sku = message.sku;
    }
    if (message.available !== 0) {
      obj.available = Math.round(message.available);
    }
    if (message.reserved !== 0) {
      obj.reserved = Math.round(message.reserved);
    }
    if (message.warehouse !== undefined) {
      obj.warehouse = WarehouseRef.toJSON(message.warehouse);
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [string, string][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StockItem>, I>>(base?: I): StockItem {
    return StockItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StockItem>, I>>(object: I): StockItem {
    const message = createBaseStockItem();
    message.sku = object.sku ?? "";
    message.available = object.available ?? 0;
    message.reserved = object.reserved ?? 0;
    message.warehouse = (object.warehouse !== undefined && object.warehouse !== null)
      ? WarehouseRef.fromPartial(object.warehouse)
      : undefined;
    message.attributes = (globalThis.Object.entries(object.attributes ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseStockItem_AttributesEntry(): StockItem_AttributesEntry {
  return { key: "", value: "" };
}

export const StockItem_AttributesEntry: MessageFns<StockItem_AttributesEntry> = {
  encode(message: StockItem_AttributesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StockItem_AttributesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStockItem_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StockItem_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: StockItem_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StockItem_AttributesEntry>, I>>(base?: I): StockItem_AttributesEntry {
    return StockItem_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StockItem_AttributesEntry>, I>>(object: I): StockItem_AttributesEntry {
    const message = createBaseStockItem_AttributesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetStockRequest(): GetStockRequest {
  return { skus: [], region: "", context: undefined, actor: undefined, headers: {} };
}

export const GetStockRequest: MessageFns<GetStockRequest> = {
  encode(message: GetStockRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.skus) {
      writer.uint32(10).string(v!);
    }
    if (message.region !== "") {
      writer.uint32(18).string(message.region);
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(26).fork()).join();
    }
    if (message.actor !== undefined) {
      Actor.encode(message.actor, writer.uint32(34).fork()).join();
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]: [string, string]) => {
      GetStockRequest_HeadersEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.skus.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.actor = Actor.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = GetStockRequest_HeadersEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.headers[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockRequest {
    return {
      skus: globalThis.Array.isArray(object?.skus) ? object.skus.map((e: any) => globalThis.String(e)) : [],
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      actor: isSet(object.actor) ? Actor.fromJSON(object.actor) : undefined,
      headers: isObject(object.headers)
        ? (globalThis.Object.entries(object.headers) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: GetStockRequest): unknown {
    const obj: any = {};
    if (message.skus?.length) {
      obj.skus = message.skus;
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.actor !== undefined) {
      obj.actor = Actor.toJSON(message.actor);
    }
    if (message.headers) {
      const entries = globalThis.Object.entries(message.headers) as [string, string][];
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStockRequest>, I>>(base?: I): GetStockRequest {
    return GetStockRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStockRequest>, I>>(object: I): GetStockRequest {
    const message = createBaseGetStockRequest();
    message.skus = object.skus?.map((e) => e) || [];
    message.region = object.region ?? "";
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.actor = (object.actor !== undefined && object.actor !== null) ? Actor.fromPartial(object.actor) : undefined;
    message.headers = (globalThis.Object.entries(object.headers ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseGetStockRequest_HeadersEntry(): GetStockRequest_HeadersEntry {
  return { key: "", value: "" };
}

export const GetStockRequest_HeadersEntry: MessageFns<GetStockRequest_HeadersEntry> = {
  encode(message: GetStockRequest_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockRequest_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockRequest_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockRequest_HeadersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: GetStockRequest_HeadersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStockRequest_HeadersEntry>, I>>(base?: I): GetStockRequest_HeadersEntry {
    return GetStockRequest_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStockRequest_HeadersEntry>, I>>(object: I): GetStockRequest_HeadersEntry {
    const message = createBaseGetStockRequest_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseGetStockResponse(): GetStockResponse {
  return { items: [], context: undefined };
}

export const GetStockResponse: MessageFns<GetStockResponse> = {
  encode(message: GetStockResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      StockItem.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(StockItem.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => StockItem.fromJSON(e)) : [],
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
    };
  },

  toJSON(message: GetStockResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => StockItem.toJSON(e));
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStockResponse>, I>>(base?: I): GetStockResponse {
    return GetStockResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStockResponse>, I>>(object: I): GetStockResponse {
    const message = createBaseGetStockResponse();
    message.items = object.items?.map((e) => StockItem.fromPartial(e)) || [];
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

function createBaseGetStockAggregatedResponse(): GetStockAggregatedResponse {
  return { responses: [] };
}

export const GetStockAggregatedResponse: MessageFns<GetStockAggregatedResponse> = {
  encode(message: GetStockAggregatedResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.responses) {
      GetStockResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetStockAggregatedResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetStockAggregatedResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.responses.push(GetStockResponse.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetStockAggregatedResponse {
    return {
      responses: globalThis.Array.isArray(object?.responses)
        ? object.responses.map((e: any) => GetStockResponse.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetStockAggregatedResponse): unknown {
    const obj: any = {};
    if (message.responses?.length) {
      obj.responses = message.responses.map((e) => GetStockResponse.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetStockAggregatedResponse>, I>>(base?: I): GetStockAggregatedResponse {
    return GetStockAggregatedResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetStockAggregatedResponse>, I>>(object: I): GetStockAggregatedResponse {
    const message = createBaseGetStockAggregatedResponse();
    message.responses = object.responses?.map((e) => GetStockResponse.fromPartial(e)) || [];
    return message;
  },
};

function createBaseReservationLine(): ReservationLine {
  return { sku: "", quantity: 0, attributes: {} };
}

export const ReservationLine: MessageFns<ReservationLine> = {
  encode(message: ReservationLine, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sku !== "") {
      writer.uint32(10).string(message.sku);
    }
    if (message.quantity !== 0) {
      writer.uint32(16).int32(message.quantity);
    }
    globalThis.Object.entries(message.attributes).forEach(([key, value]: [string, string]) => {
      ReservationLine_AttributesEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationLine {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationLine();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.sku = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.quantity = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ReservationLine_AttributesEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.attributes[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReservationLine {
    return {
      sku: isSet(object.sku) ? globalThis.String(object.sku) : "",
      quantity: isSet(object.quantity) ? globalThis.Number(object.quantity) : 0,
      attributes: isObject(object.attributes)
        ? (globalThis.Object.entries(object.attributes) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ReservationLine): unknown {
    const obj: any = {};
    if (message.sku !== "") {
      obj.sku = message.sku;
    }
    if (message.quantity !== 0) {
      obj.quantity = Math.round(message.quantity);
    }
    if (message.attributes) {
      const entries = globalThis.Object.entries(message.attributes) as [string, string][];
      if (entries.length > 0) {
        obj.attributes = {};
        entries.forEach(([k, v]) => {
          obj.attributes[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReservationLine>, I>>(base?: I): ReservationLine {
    return ReservationLine.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReservationLine>, I>>(object: I): ReservationLine {
    const message = createBaseReservationLine();
    message.sku = object.sku ?? "";
    message.quantity = object.quantity ?? 0;
    message.attributes = (globalThis.Object.entries(object.attributes ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseReservationLine_AttributesEntry(): ReservationLine_AttributesEntry {
  return { key: "", value: "" };
}

export const ReservationLine_AttributesEntry: MessageFns<ReservationLine_AttributesEntry> = {
  encode(message: ReservationLine_AttributesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReservationLine_AttributesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReservationLine_AttributesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReservationLine_AttributesEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ReservationLine_AttributesEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReservationLine_AttributesEntry>, I>>(base?: I): ReservationLine_AttributesEntry {
    return ReservationLine_AttributesEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReservationLine_AttributesEntry>, I>>(
    object: I,
  ): ReservationLine_AttributesEntry {
    const message = createBaseReservationLine_AttributesEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseReserveStockRequest(): ReserveStockRequest {
  return { reservationId: "", lines: [], region: "", context: undefined, actor: undefined, headers: {} };
}

export const ReserveStockRequest: MessageFns<ReserveStockRequest> = {
  encode(message: ReserveStockRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservationId !== "") {
      writer.uint32(10).string(message.reservationId);
    }
    for (const v of message.lines) {
      ReservationLine.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.region !== "") {
      writer.uint32(26).string(message.region);
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(34).fork()).join();
    }
    if (message.actor !== undefined) {
      Actor.encode(message.actor, writer.uint32(42).fork()).join();
    }
    globalThis.Object.entries(message.headers).forEach(([key, value]: [string, string]) => {
      ReserveStockRequest_HeadersEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveStockRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveStockRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.lines.push(ReservationLine.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.region = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.actor = Actor.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = ReserveStockRequest_HeadersEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.headers[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReserveStockRequest {
    return {
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      lines: globalThis.Array.isArray(object?.lines) ? object.lines.map((e: any) => ReservationLine.fromJSON(e)) : [],
      region: isSet(object.region) ? globalThis.String(object.region) : "",
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
      actor: isSet(object.actor) ? Actor.fromJSON(object.actor) : undefined,
      headers: isObject(object.headers)
        ? (globalThis.Object.entries(object.headers) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ReserveStockRequest): unknown {
    const obj: any = {};
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.lines?.length) {
      obj.lines = message.lines.map((e) => ReservationLine.toJSON(e));
    }
    if (message.region !== "") {
      obj.region = message.region;
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    if (message.actor !== undefined) {
      obj.actor = Actor.toJSON(message.actor);
    }
    if (message.headers) {
      const entries = globalThis.Object.entries(message.headers) as [string, string][];
      if (entries.length > 0) {
        obj.headers = {};
        entries.forEach(([k, v]) => {
          obj.headers[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReserveStockRequest>, I>>(base?: I): ReserveStockRequest {
    return ReserveStockRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReserveStockRequest>, I>>(object: I): ReserveStockRequest {
    const message = createBaseReserveStockRequest();
    message.reservationId = object.reservationId ?? "";
    message.lines = object.lines?.map((e) => ReservationLine.fromPartial(e)) || [];
    message.region = object.region ?? "";
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    message.actor = (object.actor !== undefined && object.actor !== null) ? Actor.fromPartial(object.actor) : undefined;
    message.headers = (globalThis.Object.entries(object.headers ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseReserveStockRequest_HeadersEntry(): ReserveStockRequest_HeadersEntry {
  return { key: "", value: "" };
}

export const ReserveStockRequest_HeadersEntry: MessageFns<ReserveStockRequest_HeadersEntry> = {
  encode(message: ReserveStockRequest_HeadersEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveStockRequest_HeadersEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveStockRequest_HeadersEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReserveStockRequest_HeadersEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ReserveStockRequest_HeadersEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReserveStockRequest_HeadersEntry>, I>>(
    base?: I,
  ): ReserveStockRequest_HeadersEntry {
    return ReserveStockRequest_HeadersEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReserveStockRequest_HeadersEntry>, I>>(
    object: I,
  ): ReserveStockRequest_HeadersEntry {
    const message = createBaseReserveStockRequest_HeadersEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseReserveStockResponse(): ReserveStockResponse {
  return { reservationId: "", reservedItems: [], context: undefined };
}

export const ReserveStockResponse: MessageFns<ReserveStockResponse> = {
  encode(message: ReserveStockResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reservationId !== "") {
      writer.uint32(10).string(message.reservationId);
    }
    for (const v of message.reservedItems) {
      StockItem.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.context !== undefined) {
      RequestContext.encode(message.context, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReserveStockResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReserveStockResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.reservationId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reservedItems.push(StockItem.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.context = RequestContext.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReserveStockResponse {
    return {
      reservationId: isSet(object.reservationId)
        ? globalThis.String(object.reservationId)
        : isSet(object.reservation_id)
        ? globalThis.String(object.reservation_id)
        : "",
      reservedItems: globalThis.Array.isArray(object?.reservedItems)
        ? object.reservedItems.map((e: any) => StockItem.fromJSON(e))
        : globalThis.Array.isArray(object?.reserved_items)
        ? object.reserved_items.map((e: any) => StockItem.fromJSON(e))
        : [],
      context: isSet(object.context) ? RequestContext.fromJSON(object.context) : undefined,
    };
  },

  toJSON(message: ReserveStockResponse): unknown {
    const obj: any = {};
    if (message.reservationId !== "") {
      obj.reservationId = message.reservationId;
    }
    if (message.reservedItems?.length) {
      obj.reservedItems = message.reservedItems.map((e) => StockItem.toJSON(e));
    }
    if (message.context !== undefined) {
      obj.context = RequestContext.toJSON(message.context);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReserveStockResponse>, I>>(base?: I): ReserveStockResponse {
    return ReserveStockResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReserveStockResponse>, I>>(object: I): ReserveStockResponse {
    const message = createBaseReserveStockResponse();
    message.reservationId = object.reservationId ?? "";
    message.reservedItems = object.reservedItems?.map((e) => StockItem.fromPartial(e)) || [];
    message.context = (object.context !== undefined && object.context !== null)
      ? RequestContext.fromPartial(object.context)
      : undefined;
    return message;
  },
};

export type InventoryServiceService = typeof InventoryServiceService;
export const InventoryServiceService = {
  getStock: {
    path: "/acme.inventory.v1.InventoryService/GetStock",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetStockRequest): Buffer => Buffer.from(GetStockRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetStockRequest => GetStockRequest.decode(value),
    responseSerialize: (value: GetStockResponse): Buffer => Buffer.from(GetStockResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetStockResponse => GetStockResponse.decode(value),
  },
  /** Aggregated: returns one message with N GetStockResponse entries (1–3) based on SYSTEM_B_ENABLED / SYSTEM_C_ENABLED. */
  getStockAggregated: {
    path: "/acme.inventory.v1.InventoryService/GetStockAggregated",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: GetStockRequest): Buffer => Buffer.from(GetStockRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): GetStockRequest => GetStockRequest.decode(value),
    responseSerialize: (value: GetStockAggregatedResponse): Buffer =>
      Buffer.from(GetStockAggregatedResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): GetStockAggregatedResponse => GetStockAggregatedResponse.decode(value),
  },
  reserveStock: {
    path: "/acme.inventory.v1.InventoryService/ReserveStock",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ReserveStockRequest): Buffer => Buffer.from(ReserveStockRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ReserveStockRequest => ReserveStockRequest.decode(value),
    responseSerialize: (value: ReserveStockResponse): Buffer =>
      Buffer.from(ReserveStockResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ReserveStockResponse => ReserveStockResponse.decode(value),
  },
} as const;

export interface InventoryServiceServer extends UntypedServiceImplementation {
  getStock: handleUnaryCall<GetStockRequest, GetStockResponse>;
  /** Aggregated: returns one message with N GetStockResponse entries (1–3) based on SYSTEM_B_ENABLED / SYSTEM_C_ENABLED. */
  getStockAggregated: handleUnaryCall<GetStockRequest, GetStockAggregatedResponse>;
  reserveStock: handleUnaryCall<ReserveStockRequest, ReserveStockResponse>;
}

export interface InventoryServiceClient extends Client {
  getStock(
    request: GetStockRequest,
    callback: (error: ServiceError | null, response: GetStockResponse) => void,
  ): ClientUnaryCall;
  getStock(
    request: GetStockRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetStockResponse) => void,
  ): ClientUnaryCall;
  getStock(
    request: GetStockRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetStockResponse) => void,
  ): ClientUnaryCall;
  /** Aggregated: returns one message with N GetStockResponse entries (1–3) based on SYSTEM_B_ENABLED / SYSTEM_C_ENABLED. */
  getStockAggregated(
    request: GetStockRequest,
    callback: (error: ServiceError | null, response: GetStockAggregatedResponse) => void,
  ): ClientUnaryCall;
  getStockAggregated(
    request: GetStockRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: GetStockAggregatedResponse) => void,
  ): ClientUnaryCall;
  getStockAggregated(
    request: GetStockRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: GetStockAggregatedResponse) => void,
  ): ClientUnaryCall;
  reserveStock(
    request: ReserveStockRequest,
    callback: (error: ServiceError | null, response: ReserveStockResponse) => void,
  ): ClientUnaryCall;
  reserveStock(
    request: ReserveStockRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ReserveStockResponse) => void,
  ): ClientUnaryCall;
  reserveStock(
    request: ReserveStockRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ReserveStockResponse) => void,
  ): ClientUnaryCall;
}

export const InventoryServiceClient = makeGenericClientConstructor(
  InventoryServiceService,
  "acme.inventory.v1.InventoryService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): InventoryServiceClient;
  service: typeof InventoryServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
